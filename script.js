$(document).ready(function () {
    $('#lineForm').submit(function (e) {
        // Validate if needed or show animations
        alert("Form submitted successfully. Redirecting to display details.");
    })
});